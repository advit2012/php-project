pipeline {
    agent any
    stages {
        stage('Git Clone') {
            steps {
                git url: 'https://github.com/advit2012/php-project/', branch: 'master'
            }
        }
        stage('Build Docker Image') {
            steps {
                script {
                    // Change version accordingly
                    def imageName = "advit2012/akshatnewimg2july:v2"
                    sh "docker build -t ${imageName} ."
                    sh 'docker images'
                }
            }
        }
        stage('Docker Login and Push') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'advit2012', passwordVariable: 'PASS', usernameVariable: 'USER')]) {
                    sh "echo $PASS | docker login -u $USER --password-stdin"
                    sh "docker push ${imageName}"
                }
            }
        }
        stage('Deploy') {
            steps {
                script {
                    def dockerrm = 'sudo docker rm -f My-first-containe2211 || true'
                    def dockerPull = 'sudo docker pull advit2012/akshatnewimg2july:v2'  // Use the correct image tag
                    def dockerCmd = 'sudo docker run -itd --name My-first-containe2211 -p 8083:80 advit2012/akshatnewimg2july:v2'
                    
                    sshagent(['sshkeypair']) {
                        // Remove existing container if exists
                        sh "ssh -o StrictHostKeyChecking=no ubuntu@172.31.42.229 ${dockerrm}"
                        // Pull the latest Docker image
                        sh "ssh -o StrictHostKeyChecking=no ubuntu@172.31.42.229 ${dockerPull}"
                        // Run the new container
                        sh "ssh -o StrictHostKeyChecking=no ubuntu@172.31.42.229 ${dockerCmd}"
                    }
                }
            }
        }
    }
}
